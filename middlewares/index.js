const validation = require("./validation");
const authenticate = require("./authenticate");
const uploadAvatar = require("./uploadAvatar");

module.exports = { validation, authenticate, uploadAvatar };
